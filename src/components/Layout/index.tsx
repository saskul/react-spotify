import React from 'react';
import './Layout.scss';

type Props = {};
type State = {};

class Layout extends React.Component<Props, State> {
  render() {
    return (
      <div>Layout</div>
    );
  }
}

export default Layout;